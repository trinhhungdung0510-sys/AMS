/**
 * @deprecated Use zoneGeometry.js — kept for backward-compatible imports.
 */
export {
  getImageMetrics,
  imagePixelToScreenPoint as imageToScreenPoint,
  normalizedToScreenPoint,
  pointsToSvgString,
  roundNormalizedPoint as roundPoint,
  screenToImagePixel as screenToImagePoint,
  screenToNormalizedPoint,
} from './zoneGeometry'
